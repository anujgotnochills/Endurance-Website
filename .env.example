# No backend services or API keys are currently required.
# The contact form uses WhatsApp directly (no server needed).
#
# If you add a new service in the future, add its keys here
# and copy this file to .env (which is gitignored).
#
# Example:
# VITE_MY_SERVICE_KEY=your_key_here
